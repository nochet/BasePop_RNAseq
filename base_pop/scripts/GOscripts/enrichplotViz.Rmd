---
title: "EnrichPlot"
author: "Enoch Ng'oma"
date: "1/30/2019"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)

# https://bioconductor.org/packages/release/bioc/vignettes/enrichplot/inst/doc/enrichplot.html
library(enrichplot)
library(DOSE)

```

```{r}
# Load data with entrez ids
load(file="../../processed/DESEQ/lrt.treatment_entrez.Rda")
head(rownames(lrt.treatment))
head(lrt.treatment, 10)
head(lrt.treatment$symbol)

sig.lrt <- subset(lrt.treatment, lrt.treatment$padj<0.05)
all.genes <- sig.lrt[,"entrez"]

```

```{r}
names(all.genes) <- NULL
c <- all.genes[order(all.genes, decreasing = TRUE)]
c = sort(all.genes, decreasing = TRUE)
c <- gseDO(all.genes)
c <- enrichDGN(all.genes, organism="org.Dm.eg.db")

edo <- enrichDGN(de)
```

